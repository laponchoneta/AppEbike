name: CI Buildozer APK

on:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            git zip unzip \
            openjdk-17-jdk \
            autoconf automake libtool \
            pkg-config cmake \
            zlib1g-dev \
            libncurses5-dev libncursesw5-dev libtinfo5 \
            libffi-dev libssl-dev \
            libltdl-dev \
            ccache

      - name: Install legacy Android NDK r21e
        run: |
          cd $HOME
          wget -q https://dl.google.com/android/repository/android-ndk-r21e-linux-x86_64.zip
          unzip -q android-ndk-r21e-linux-x86_64.zip
          echo "LEGACY_NDK=$HOME/android-ndk-r21e" >> $GITHUB_ENV
          echo "LEGACY_NDK=$HOME/android-ndk-r21e"
          ls -la $HOME/android-ndk-r21e

      - name: Build APK
        run: |
          buildozer -v android debug

      - name: Check NDKs
        run: |
          echo "ANDROIDNDK=$ANDROIDNDK"
          echo "LEGACY_NDK=$LEGACY_NDK"
          ls -la /home/runner/.buildozer/android/platform/

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: package
          path: bin/*.apk